要点：
- 简单线性回归模型
	- 特征
	- 标签
	- 偏差（截距）
	- 权重
	- 噪声
		- 高斯噪声
- 梯度的可视化理解
- 梯度下降
	- 初始化参数
		- 随机初始化
	- 前向传递
	- 计算误差和损失
		- 误差
		- 损失
		- 损失函数（回归和分类）
	- 计算梯度
	- 更新参数
- 随机梯度下降，批量batch梯度下降，小批量mini-batch梯度下降
- 超参数
	- 批量batch，小批量mini-batch
	- 学习率对损失的影响
- 标准化，缩放特征

---


简单线性回归模型的比喻（工资）


生成数据的方法：

线性+噪声

高斯噪声
- 正态分布（均值0，方差1）

否则，合成数据集将是一条完美的直线。可以使用Numpy的 randn方法生成噪声，该方法从正态分布(均值0和方差1)中抽取样本，然后将其乘以一个因子以调整噪声程度。


伪随机数

将生成的数据拆分为训练集和验证集。如：打乱索引数组，并使用前80个打乱的点进行训练
- 在训练模型之前总是对数据点进行打乱，以提高梯度下降的性能。
- 一个例外：时间序列问题，打乱会导致数据泄露

---

梯度下降步骤
0. 初始化参数
	- 随机初始化
1. 前向传递
2. 计算误差和损失
	- 误差
	- 损失
	- 损失函数（回归和分类）
3. 计算梯度
	- 梯度和偏导数定义
	- 反向传播
4. 更新参数
	- 学习率
		- 步幅
		- 大小对学习的影响
		- 学习率的大小受到最陡曲线的限制
- 缩放，标准化和归一化（预处理）
	- 坏特征对学习的影响
	- 方式
		- 计算均值和方差
		- 缩放特征
		- ![[Pasted image 20260425171330.png]]
	- 零均值和单位标准差的好处
		- 所有数值比例相似，不受每个特征原范围影响
		- 提高梯度下降性能
		- 缓解梯度消失（零均值的好处）
	- 预处理步骤注意点
		- 必须在训练→验证→测试拆分之后执行；否则，您将把验证集和/或测试集的信息泄露给模型。
		- 在仅使用训练集来拟合StandardScaler之后，应该使用其transform方法将预处理步骤应用于所有数据集：训练、验证和测试。

梯度下降的分类
- 随机梯度下降，批量batch梯度下降，小批量mini-batch梯度下降


循环往复
- Epoch周期的定义
- 批量，随机，小批量和周期的关系
- 训练和周期的关系

梯度下降的路径
- 小批量梯度下降能够更接近最小点(使用相同数量的时期)，受益于比批量梯度下降更多的更新
- 随机梯度下降路径有点奇怪：它在第1个周期结束时已经非常接近最小点了，但随后似乎无法真正达到它。但这是意料之中的，因为它每次更新都使用一个数据点，它永远不会稳定，并且永远徘徊在最小点附近。
- *权衡：要么有一个稳定和平滑的轨迹，要么更快地朝着最小值移动*





其他代码：
- 随机函数
	- `np.random.shuffle(idx)`
	- `np.random.rand(N,1)` 方差1
- 损失面（仅用于教学目的，需要代码引导）
	- `np.arange(N)`
	- `np.linspace(true_b-3, true_b+3, 101)`
	- `np.arange(11).mean()`
	- ```python
	  all_predictions = np.apply_along_axis(  
						    func1d=lambda x: bs + ws * x, # 函数   
						    axis=1,   # 0是行，1是列
						    arr=x_train) # 
	  ```
	- ![[Pasted image 20260425161919.png]]
- 可视化梯度
	- 横截面
	- ![[Pasted image 20260425162540.png]]
	- ![[Pasted image 20260425162549.png]]
	- 有个注意点：明显b比w对变化更加敏感
- 学习率的选择
	- 对于学习率来说，太大是一个相对概念：它取决于曲线有多陡，或者换句话说，它取决于梯度有多大。
	- 确实有许多曲线、许多梯度：每个参数一个。但是只有一个学习率可供选择。
	- 这意味着学习率的大小受到最陡曲线的限制。其他所有曲线都必须效仿，也就是说，考虑到它们的形状，将使用次优的学习率。
	- **合理的结论是：最好是所有的曲线都同样陡峭，这样所有曲线的学习率都更接近**最优。
- 缩放，标准化和归一化
	- 缩放后会得到碗装图形：代表着**最好的表面：横截面同样陡峭，**
	- 始终标准化(缩放)使用的特征。
	-![[Pasted image 20260425172110.png]]






