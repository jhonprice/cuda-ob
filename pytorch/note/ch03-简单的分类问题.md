要点：
- **二元分类**模型
- **logit**和概率
- **二元交叉熵损失**
- 使用损失函数处理**不平衡的数据集**
- **决策边界**
- **可分离性**
- **分类阈值**的选择和评估指标
- **ROC**和**精确率-召回率**曲线
---
**分类问题**

二元分类问题如下：
![[Pasted image 20260507220432.png]]
分类模型中，输出的是正类的**预测概率**

---
**数据生成**

到目前为止:
- 生成的数据都是一维的：使用`numpy`

现在，将特征扩展至二维（其实就是2个特征`x1`和`x2`）
- 使用`scikit-learn`的`make_moons`生成一个包含100个数据点的小数据集
- 使用`scikit-learn`的`train_test_split`执行**训练-验证拆分**（之前是`pytorch`的`random_split`）
- **高斯噪声**还是要添加的
- 对特征进行**标准化**也是添加的
![[Pasted image 20260508221738.png|697]]
训练验证拆分
```python
_train,X_val,y_train,y_val = train_test_split(X,y,test_size=.2,random_state=13)

```
- `test_size`参数指定了测试集占总数据集的比例
- `random_state`参数确保了划分的可重复性，即随机数生成器的种子

[[ch00-梯度下降]]中提过标准化的内容，相关代码
```python
sc = Standardscaler()
sc.fit(X_train)

X_train = sc.transform(X_train)
X_val = sc.transform(X_val)
```

>**只对训练集 fit这个StandardScaler**,然后使用它的 transform方法将预处理步骤应用于所有数据集：训练、验证和测试。否则，您会将验证集和/或测试集的信息泄露给自己的模型。

---
**数据准备**

构建`TensorDatasets`，并构建`DataLoader`，数据准备将`numpy`转换为`pytorch`张量
- 两个特征`x1`和`x2`，标签是`y`是0（红色）或1（蓝色）

```python
torch.manual_seed(13)

#从Numpy数组构建张量

x_train_tensor = torch.as_tensor(X_train).float()
y_train_tensor = torch.as_tensor(y_train.reshape(-1,1)).float()

x_val_tensor = torch.as_tensor(X_val).float()
y_val_tensor = torch.as_tensor(y_val.reshape(-1,1)).float()


#构建包含所有数据点的数据集

train_dataset = TensorDataset(x_train_tensor,y_train_tensor)
val_dataset = TensorDataset(x_val_tensor,y_val_tensor)

#构建每个集合的加载器

train_loader = DataLoader(dataset=train_dataset, batch_size=16,shuffle=True)
val_loader = DataLoader(dataset=val_dataset, batch_size=16)
```

---
**模型**

