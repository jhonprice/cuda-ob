> 当标识符在表达式中使用时，该标识符必须与其定义相连接
> - 如果编译器在编译的文件中既未看到标识符的前向声明也未看到其定义，它将在使用该标识符的位置报错
> - 否则，若同一文件中存在定义，编译器会将标识符的使用与其定义关联起来。
> - 否则，如果定义存在于另一个文件中（且对链接器可见），链接器会将标识符的使用与其定义连接起来
> - 否则，链接器将报错，提示找不到该标识符的定义。
> 


