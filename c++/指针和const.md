- 一个非 const 指针（例如 `int* ptr` ）可以被赋予另一个地址，以改变它所指向的内容。
- 一个 const 指针（例如 `int* const ptr` ）始终指向同一个地址，并且这个地址不能被改变。
- 一个指向非 const 值的指针（例如 `int* ptr` ）可以改变它所指向的值。这些指针不能指向 const 值。
- 一个指向 const 值的指针（例如 `const int* ptr` ）在通过指针访问时将值视为 const，因此不能改变它所指向的值。这些指针可以指向 const 或非 const 左值（但不能指向右值，因为*右值没有地址*）。