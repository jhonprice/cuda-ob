**局部变量**具有**块作用域**，这意味着它们的作用范围从其定义点开始，到其所在块的结束为止。

变量的**存储期**（通常简称为"**期**"）决定了管理变量何时以及如何被创建（实例化）和销毁的规则。在大多数情况下，变量的存储期直接决定了其 `lifetime` 。
- 局部变量具有**自动存储期**，这意味着它们在定义点被创建，并在其定义的块结束时被销毁。
- 局部变量有时被称为**自动变量**。


在外部代码块中定义的变量可以在嵌套代码块内部访问


