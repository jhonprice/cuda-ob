ODR 规定类型、模板、内联函数和内联变量允许在不同文件中具有相同的定义。因此，即使模板定义被复制到多个文件中（只要每个定义完全相同），也不会产生问题

从函数模板**隐式实例化**的函数是**隐式内联**的，因此它们可以在多个文件中定义，只要每个定义完全相同即可。

*模板本身不是内联的*，因为内联的概念只适用于变量和函数。


> 在多个文件中需要的模板应在头文件中定义，然后通过 `#include` 在需要的地方包含。这样可以让编译器看到完整的模板定义，并在需要时实例化模板。