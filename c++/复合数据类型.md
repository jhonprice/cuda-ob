**复合数据类型**（有时也称为**组合数据类型**），这些类型是根据其他现有数据类型定义的。
C++支持以下复合类型：
- 函数
- C 风格数组
- **指针**类型：
    - 指向对象的指针
    - 指向函数的指针
- 指向成员的类型：
    - 指向数据成员的指针
    - 指向成员函数的指针
- **引用类型**：
    - **左值引用**
    - **右值引用**
- **枚举类型**：
    - 无作用域枚举
    - 作用域枚举
- Class types:  **类类型**
	- 类类型是指结构体、类或联合体类型

